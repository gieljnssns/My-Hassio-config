

sudo raspi-config
  expand filesystem

sudo reboot

sudo apt update
sudo apt upgrade -y
sudo apt install git libcamera0 libfreetype6
git clone https://github.com/gieljnssns/dotfiles-raspi.git
cd dotfiles-raspi
chmod +x install.sh
./install.sh

sudo reboot

# FFMPEG
# https://jollejolles.com/installing-ffmpeg-with-h264-support-on-raspberry-pi/

# MediaMTX
wget https://github.com/bluenviron/mediamtx/releases/download/v1.9.3/mediamtx_v1.9.3_linux_armv7.tar.gz
tar -xvzf mediamtx_v1.9.3_linux_armv7.tar.gz

```sh
sudo mv mediamtx /usr/local/bin/
sudo rm mediamtx.yml
sudo nano /usr/local/etc/mediamtx.yml
/Users/giel/Dev/My-Hassio/documentation/Hidden/mediamtx.yml
```

Create a _systemd_ service:

```sh
sudo tee /etc/systemd/system/mediamtx.service >/dev/null << EOF
[Unit]
Wants=network.target
[Service]
ExecStart=/usr/local/bin/mediamtx /usr/local/etc/mediamtx.yml
[Install]
WantedBy=multi-user.target
EOF
```

Enable and start the service:

```sh
sudo systemctl daemon-reload
sudo systemctl enable mediamtx
sudo systemctl start mediamtx
sudo systemctl status mediamtx
sudo systemctl restart mediamtx

sudo journalctl -u mediamtx.service
```