  - platform: template
    sensors:
      netatmo_thermostat_online:
        friendly_name: "Thermostaat"
        value_template: >
          {% if states.device_tracker.netatmo_thermostat %}
            {% if is_state('device_tracker.netatmo_thermostat_ping', 'home') %}
              Online
            {% else %}
              Offline
            {% endif %}
          {% else %}
            Unknown
          {% endif %}
          
      netatmo_presence_online:
        friendly_name: "Presence"
        value_template: >
          {% if states.device_tracker.netatmo_presence_ping %}
            {% if is_state('device_tracker.netatmo_presence_ping', 'home') %}
              Online
            {% else %}
              Offline
            {% endif %}
          {% else %}
            Unknown
          {% endif %}

      kostal_online:
        friendly_name: "Kostal"
        value_template: >
          {% if states.device_tracker.kostal_ping %}
            {% if is_state('device_tracker.kostal_ping', 'home') %}
              Online
            {% else %}
              Offline
            {% endif %}
          {% else %}
            Unknown
          {% endif %}

      smappee_online:
        friendly_name: "Smappee"
        value_template: >
          {% if states.device_tracker.smappee_ping %}
            {% if is_state('device_tracker.smappee_ping', 'home') %}
              Online
            {% else %}
              Offline
            {% endif %}
          {% else %}
            Unknown
          {% endif %}

      hue_beneden_online:
        friendly_name: "Hue beneden"
        value_template: >
          {% if states.device_tracker.hue_beneden_ping %}
            {% if is_state('device_tracker.hue_beneden_ping', 'home') %}
              Online
            {% else %}
              Offline
            {% endif %}
          {% else %}
            Unknown
          {% endif %}

      tvpi_online:
        friendly_name: "TvPi"
        value_template: >
          {% if states.device_tracker.tvpi_ping %}
            {% if is_state('device_tracker.tvpi_ping', 'home') %}
              Online
            {% else %}
              Offline
            {% endif %}
          {% else %}
            Unknown
          {% endif %}

      sonos_keuken_online:
        friendly_name: "Sonos keuken"
        value_template: >
          {% if states.device_tracker.sonos_keuken_ping %}
            {% if is_state('device_tracker.sonos_keuken_ping', 'home') %}
              Online
            {% else %}
              Offline
            {% endif %}
          {% else %}
            Unknown
          {% endif %}

      sonos_badkamer_online:
        friendly_name: "Sonos badkamer"
        value_template: >
          {% if states.device_tracker.sonos_badkamer_ping %}
            {% if is_state('device_tracker.sonos_badkamer_ping', 'home') %}
              Online
            {% else %}
              Offline
            {% endif %}
          {% else %}
            Unknown
          {% endif %}

      piholepi_online:
        friendly_name: "PiHole"
        value_template: >
          {% if states.device_tracker.piholepi_ping %}
            {% if is_state('device_tracker.piholepi_ping', 'home') %}
              Online
            {% else %}
              Offline
            {% endif %}
          {% else %}
            Unknown
          {% endif %}

      hue_boven_online:
        friendly_name: "Hue boven"
        value_template: >
          {% if states.device_tracker.hue_boven_ping %}
            {% if is_state('device_tracker.hue_boven_ping', 'home') %}
              Online
            {% else %}
              Offline
            {% endif %}
          {% else %}
            Unknown
          {% endif %}

      mysensors_gw_online:
        friendly_name: "MySensors"
        value_template: >
          {% if states.device_tracker.mysensors_gw_ping %}
            {% if is_state('device_tracker.mysensors_gw_ping', 'home') %}
              Online
            {% else %}
              Offline
            {% endif %}
          {% else %}
            Unknown
          {% endif %}

      tahoma_online:
        friendly_name: "Tahoma"
        value_template: >
          {% if states.device_tracker.tahoma_ping %}
            {% if is_state('device_tracker.tahoma_ping', 'home') %}
              Online
            {% else %}
              Offline
            {% endif %}
          {% else %}
            Unknown
          {% endif %}

      poortpi_online:
        friendly_name: "PoortPi"
        value_template: >
          {% if states.device_tracker.poortpi_ping %}
            {% if is_state('device_tracker.poortpi_ping', 'home') %}
              Online
            {% else %}
              Offline
            {% endif %}
          {% else %}
            Unknown
          {% endif %}

      pizero_boven_online:
        friendly_name: "PiZero boven"
        value_template: >
          {% if states.device_tracker.pizero_boven_ping %}
            {% if is_state('device_tracker.pizero_boven_ping', 'home') %}
              Online
            {% else %}
              Offline
            {% endif %}
          {% else %}
            Unknown
          {% endif %}

      pizero_garage_online:
        friendly_name: "PiZero garage"
        value_template: >
          {% if states.device_tracker.pizero_garage_ping %}
            {% if is_state('device_tracker.pizero_garage_ping', 'home') %}
              Online
            {% else %}
              Offline
            {% endif %}
          {% else %}
            Unknown
          {% endif %}