  - platform: generic_thermostat
    name: Noor
    heater: switch.noor
    target_sensor: sensor.temperatuur_noor
    min_temp: 10
    max_temp: 24
    target_temp: 10
    away_temp: 10
    cold_tolerance: 0.3
    hot_tolerance: 0

  - platform: generic_thermostat
    name: Fien
    heater: switch.fien
    target_sensor: sensor.temperatuur_fien
    min_temp: 10
    max_temp: 24
    target_temp: 10
    away_temp: 10
    cold_tolerance: 0.3
    hot_tolerance: 0

  - platform: generic_thermostat
    name: Badkamer
    heater: switch.badkamer
    target_sensor: sensor.temperatuur_badkamer
    min_temp: 10
    max_temp: 24
    target_temp: 10
    away_temp: 10
    cold_tolerance: 0.3
    hot_tolerance: 0

  - platform: generic_thermostat
    name: Slaapkamer
    heater: switch.slaapkamer
    target_sensor: sensor.temperatuur_slaapkamer
    min_temp: 10
    max_temp: 24
    target_temp: 10
    away_temp: 10
    cold_tolerance: 0.3
    hot_tolerance: 0

  - platform: netatmo
