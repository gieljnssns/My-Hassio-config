  discovery: true
  host_ip: !secret ha_ip
  igmp_fix: true
  scan_interval: 5
  sensors:
    - current_consumption
    - total_consumption
    - switch
  # discovery: true
  # igmp_fix: true
  # scan_interval: 5
  # sensors:
  #   - power
  #   - switch
