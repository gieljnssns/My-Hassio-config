- binary_sensor:
    - name: "Living grouped not master"
      unique_id: 947a4957-1cbf-43be-9fb9-07e996614dbb
      state: >
        {{ state_attr('media_player.living_2', 'group_members')[0] | string != 'media_player.living_2'
          and state_attr('media_player.living_2', 'group_members') | length > 1 }}

    # - binary_sensor:
    - name: "Keuken grouped not master"
      unique_id: 3eba5ed0-61a1-4682-bc9e-8ddabf895198
      state: >
        {{ state_attr('media_player.keuken', 'group_members')[0] | string != 'media_player.keuken'
          and state_attr('media_player.keuken', 'group_members') | length > 1 }}

    # - binary_sensor:
    - name: "Badkamer grouped not master"
      unique_id: 5fc66a63-1faf-47c6-baa7-f1c9031c3415
      state: >
        {{ state_attr('media_player.badkamer', 'group_members')[0] | string != 'media_player.badkamer'
          and state_attr('media_player.badkamer', 'group_members') | length > 1 }}

    # - binary_sensor:
    - name: "2C grouped not master"
      unique_id: 4f1e29e5-c949-48e1-bc69-0554fb1e2091
      state: >
        {{ state_attr('media_player.2c', 'group_members')[0] | string != 'media_player.2c'
          and state_attr('media_player.2c', 'group_members') | length > 1 }}

    # - binary_sensor:
    - name: "Move grouped not master"
      unique_id: 437ef410-0390-4ae6-97d6-06663733fa29
      state: >
        {{ state_attr('media_player.move', 'group_members')[0] | string != 'media_player.move'
          and state_attr('media_player.move', 'group_members') | length > 1 }}
