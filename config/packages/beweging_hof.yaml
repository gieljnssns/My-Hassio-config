input_boolean:
  beweging_hof:

timer:
  timer_beweging_hof:
    duration: '00:05:00'

binary_sensor:
  - platform: template
    sensors:
      beweging_hof:
        device_class: occupancy
        friendly_name: "Beweging hof"
        value_template: >-
          {{ is_state('input_boolean.beweging_hof', 'on') }}
