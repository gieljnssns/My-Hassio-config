input_boolean:
  beweging_noor:

timer:
  timer_beweging_noor:
    duration: '00:04:00'

binary_sensor:
  - platform: template
    sensors:
      beweging_noor:
        device_class: occupancy
        friendly_name: "Beweging noor"
        value_template: >-
          {{ is_state('input_boolean.beweging_noor', 'on') }}
