mqtt_arrive_script:
  sequence:
  - service: script.mqtt_arrive_trigger
    data_template:
      identity: '{{ identity }}'
  - delay: 00:00:15
  - service: script.mqtt_arrive_trigger
    data_template:
      identity: '{{ identity }}'
  - delay: 00:00:15
  - service: script.mqtt_arrive_trigger
    data_template:
      identity: '{{ identity }}'
  - delay: 00:00:15
  - service: script.mqtt_arrive_trigger
    data_template:
      identity: '{{ identity }}'
  - delay: 00:00:15
  - service: script.mqtt_arrive_trigger
    data_template:
      identity: '{{ identity }}'
  - delay: 00:00:15
  - service: script.mqtt_arrive_trigger
    data_template:
      identity: '{{ identity }}'
  - delay: 00:00:15
  - service: script.mqtt_arrive_trigger
    data_template:
      identity: '{{ identity }}'