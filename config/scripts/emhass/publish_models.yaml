emhass_publish_models:
  alias: EMHASS publish models
  mode: single
  sequence:
    # --- RIDGE ---
    - action: rest_command.publish_data
      data:
        prefix: "ridge_"
    - delay: "00:00:01"
