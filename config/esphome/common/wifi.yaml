wifi:
  ssid: !secret ssid
  id: ssid1
  password: !secret password
  power_save_mode: none
  manual_ip:
    static_ip: ${manual_ip}
    gateway: 192.168.89.1
    subnet: 255.255.255.0
  # ap:
  #   ssid: ${devicename}
  #   password: !secret password