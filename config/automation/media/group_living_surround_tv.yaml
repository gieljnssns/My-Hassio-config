alias: Sonos living join
trigger:
  - platform: template
    value_template: >
      {% if is_state_attr('media_player.living_2', 'source', 'TV') %}
        true
      {% endif %}
action:
  - service: sonos.join
    data:
      master: media_player.living_2
      entity_id: media_player.living_surround
