alias: Zonnetent inschuiven #Sunscreen up/inklappen
id: 48a786d1-0219-4da1-825b-b67e5a770a8d
trigger:
  # Sun based triggers
  - platform: numeric_state
    entity_id: sensor.sun_azimut
    above: 330
  - platform: numeric_state
    entity_id: sensor.sun_elevation
    below: -10
  - platform: state
    entity_id: sensor.dark_inside
    to: "true"
    for:
      minutes: 5
  - platform: numeric_state
    entity_id: sensor.buiten_lichtsterkte
    below: 10000
    for:
      minutes: 5

  # Weather based triggers
  - platform: template
    value_template: >
      {{ states("sensor.windsnelheid") | int(0) > 26 }}
  - platform: template
    value_template: >
      {{ states("sensor.windvlaag") | int(0) > 40 }}
  # - platform: template
  #   value_template: >
  #     {{ states("sensor.buiten_lichtsterkte") | float(0) < 4000 and
  #       (states("sensor.solcast_forecast_next_hour") | float(0) < 1000 or
  #       states("sensor.solcast_forecast_this_hour") | float(0) < 1000) }}

  - platform: template
    value_template: >
      {% if states("sensor.neerslag_komend_uur_zandhoven") != "unknown" and states("sensor.neerslag_komend_uur_zandhoven") != "unavailable" %}
        {{ states("sensor.neerslag_komend_uur_zandhoven") | float > 0.4 }}
      {% else %}
        {{ states("sensor.neerslagverwachting_gemiddeld")  | float > 0.4 }}
      {% endif %}
  - platform: template
    value_template: >
      {% if states("sensor.neerslag_zandhoven") != "unknown" and states("sensor.neerslag_zandhoven") != "unavailable" %}
        {{ states("sensor.neerslag_zandhoven") | float > 0.15 }}
      {% else %}
        {{ states("sensor.neerslagintensiteit")  | float > 0.15 }}
      {% endif %}

  - platform: state
    entity_id: binary_sensor.zonnetent_vibratie
    to: "on"

condition:
  condition: or
  conditions:
    - condition: template
      value_template: >
        {{ state_attr("cover.zonnetent", "current_position") > 10 }}

actions:
  - action: cover.close_cover
    data: {}
    target:
      entity_id: cover.zonnetent
  # - action: notify.mobile_app_gphone
  #   data_template:
  #     message: "De zonnetent is ingeschoven, reden {{trigger.entity_id}} "
  - delay:
      minutes: 3
  - action: cover.close_cover
    data: {}
    target:
      entity_id: cover.zonnetent
