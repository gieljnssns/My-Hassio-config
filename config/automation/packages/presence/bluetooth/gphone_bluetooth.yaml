  alias: gPhone bluetooth
  initial_state: 'on' 
  trigger:
    - platform: state
      entity_id: sensor.gphone_bt_thuis
  condition:
    condition: and
    conditions:
      - condition: numeric_state
        entity_id: sensor.uptime_in_uren
        above: 0.06
  action:
    - service_template: >
        {% if states.sensor.gphone_bt_thuis.state | float < 43 %}
           script.giel_bt_not_home
        {% else %}
           script.giel_bt_home
        {% endif %}
      # entity_id: input_boolean.gphone_bluetooth