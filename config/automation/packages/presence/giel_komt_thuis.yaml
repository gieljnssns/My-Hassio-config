  alias: Giel komt thuis
  initial_state: 'on' 
  trigger:
    # - platform: state
    #   entity_id: device_tracker.gphone
    #   to: 'home'
    - platform: state
      entity_id: input_boolean.gielhomekit
      to: 'on'
    - platform: state
      entity_id: input_boolean.gphone_bluetooth
      to: 'on'
  condition:
    - condition: state
      entity_id: input_boolean.gielthuis
      state: "off"
  action:
    - service: input_boolean.turn_on
      entity_id:
        - input_boolean.gielthuis
    - service: script.request_gphone_update
    # - condition: and
    #   conditions:
    #     - condition: template
    #       value_template: "{{ trigger.entity_id == 'device_tracker.gphone' }}"
    # - service: script.mqtt_arrive_script
    #   data_template:
    #       identity: '{"identity":"gPhone"}'
    # - service_template: >
    #     {% if trigger.entity_id == 'device_tracker.gphone' %}
    #       script.mqtt_arrive_script
    #     {% endif %}
    #   data_template:
    #       identity: '{"identity":"gPhone"}'
    # - service: mqtt.publish
    #   data_template:
    #     topic: 'location/giel' 
    #     payload: 'home'