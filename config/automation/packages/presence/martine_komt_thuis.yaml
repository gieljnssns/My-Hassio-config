  alias: Martine komt thuis
  trigger:
    # - platform: state
    #   entity_id: device_tracker.mphone
    #   to: 'home'
    - platform: state
      entity_id: input_boolean.martinehomekit
      to: 'on'
    - platform: state
      entity_id: input_boolean.mphone_bluetooth
      to: 'on'
  condition:
    - condition: state
      entity_id: input_boolean.martinethuis
      state: "off"
  action:
    - service: input_boolean.turn_on
      entity_id:
        - input_boolean.martinethuis
    - service: script.request_mphone_update
    # - condition: and
    #   conditions:
    #     - condition: template
    #       value_template: "{{ trigger.entity_id == 'device_tracker.mphone' }}"
    # - service: script.mqtt_arrive_script
    #   data_template:
    #       identity: '{"identity":"mPhone"}'
    # - service_template: >
    #     {% if trigger.entity_id == 'device_tracker.mphone' %}
    #       script.mqtt_arrive_script
    #     {% endif %}
    #   data_template:
    #       identity: '{"identity":"mPhone"}'
    # - service: mqtt.publish
    #   data_template:
    #     topic: 'location/martine' 
    #     payload: 'home'