  alias: 'Alarm arm night'
  initial_state: 'on' 
  trigger:
    - platform: state
      entity_id: media_player.philips_tv
      from: 'on'
      to: 'off'
    - platform: state
      entity_id: media_player.vsx_921
      from: 'on'
      to: 'off'
    - platform: state
      entity_id: light.slaapkamer
      to: 'off'
    - platform: state
      entity_id: light.slaapkamer
      to: 'unavailable'
    - platform: state
      entity_id: media_player.philips_tv_wifi
      from: 'on'
      to: 'off'
  condition:
    condition: and
    conditions:
      - condition: time
        after: '20:15:00'
        before: '04:00:00'
      - condition: template
        value_template: "{{ states('media_player.vsx_921') != 'on' }}"
      - condition: template
        value_template: "{{ states('media_player.philips_tv_wifi') != 'on' }}"
      - condition: template
        value_template: "{{ states('media_player.philips_tv') != 'on' }}"
      - condition: or
        conditions:
          # - condition: state
          #   entity_id: input_boolean.gielthuis
          #   state: 'on'
          # - condition: state
          #   entity_id: input_boolean.martinethuis
          #   state: 'on'
          - condition: or
            conditions:
              - condition: state
                entity_id: device_tracker.martine_not_so_binary
                state: 'Thuis'
              - condition: state
                entity_id: device_tracker.martine_not_so_binary
                state: 'Juist Aangekomen'
          - condition: or
            conditions:
              - condition: state
                entity_id: device_tracker.giel_not_so_binary
                state: 'Thuis'
              - condition: state
                entity_id: device_tracker.giel_not_so_binary
                state: 'Juist Aangekomen'
  action:
    - service: light.turn_on
      entity_id: light.slaapkamer
    - service: alarm_control_panel.alarm_arm_night
      entity_id: alarm_control_panel.ha_alarm
      data:
        code: !secret alarm_code
    - service: homeassistant.turn_off
      entity_id: script.mqtt_arrive_script, script.mqtt_depart_script
    - service: script.mqtt_arrive_script
      data_template:
        identity: '{"identity":"Alarm night {{now().strftime("%H:%M")}}"}'


    
    
