  alias: 'Verlichting Trap Aan/Uit'
  initial_state: 'on' 
  trigger:
    - platform: sun
      event: sunset
    - platform: sun
      event: sunrise
  # condition:
  #   condition: or
  #   conditions:
  #     - condition: state
  #       entity_id: binary_sensor.giel_bayesian
  #       state:  'on'
  #     - condition: state
  #       entity_id: binary_sensor.martine_bayesian
  #       state:  'on'
  action:
    - service_template: >
        {% if now().hour < 11  %}
          light.turn_off
        {% else %}
          light.turn_on
        {% endif %}
      entity_id: light.trap