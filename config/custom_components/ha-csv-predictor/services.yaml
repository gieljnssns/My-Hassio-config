predict:
  name: Predict sensor
  description: Predicts your sensor to a given value.
  target:
    entity:
      integration: ha_csv_predictor
      # example: sensor.predict_heating_hours
  fields:
    new_variables:
      name: New variables
      description: The new independed variables to predict the depended variable.
      example: "[2.12, 3.56]"
      required: true
      selector:
        text:
# service: ha_csv_predictor.predict
# data:
#   new_variables:
#     - 12.21
#     - 6.395
# target:
#   entity_id: sensor.test

# service: ha_csv_predictor.predict
# data:
#   new_variables:
#     dd: 12.21
#     solar: 6.395
# target:
#   entity_id: sensor.test
