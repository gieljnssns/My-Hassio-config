{
  "config": {
    "step": {
      "user": {
        "title": "Hermes Agent",
        "description": "Enter the connection details for your Hermes Agent instance.",
        "data": {
          "host": "Host",
          "port": "Port",
          "profile": "Profile",
          "profile_route": "Profile route type",
          "api_key": "API Key",
          "use_ssl": "Use HTTPS",
          "verify_ssl": "Verify SSL certificate"
        },
        "data_description": {
          "profile": "Optional route name. Add-on names use letters, numbers, and single underscores; native names follow Hermes lowercase profile IDs. Leave blank for the root API.",
          "profile_route": "Home Assistant add-on uses /profile/<name>; native Hermes multiplexer uses /p/<name>. A blank Profile always uses the root API.",
          "api_key": "API key. Native multiplexer routes require the selected profile's own API key."
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot connect to Hermes Agent. Check host, port, profile route type, and profile.",
      "invalid_auth": "Invalid API key.",
      "invalid_host": "Invalid host. Enter a DNS name or IP address only, without a scheme, path, user info, query, or fragment.",
      "invalid_profile": "Invalid profile for the selected route type. Add-on profiles use letters, numbers, and single underscores; native profiles use lowercase letters, numbers, hyphens, and underscores.",
      "invalid_profile_route": "Invalid profile route type. Select Home Assistant add-on or native Hermes multiplexer.",
      "unknown": "An unexpected error occurred."
    },
    "abort": {
      "already_configured": "This Hermes Agent endpoint and profile are already configured."
    }
  },
  "options": {
    "error": {
      "already_configured": "This Hermes Agent endpoint and profile are already configured.",
      "cannot_connect": "Cannot connect to Hermes Agent. Check host, port, profile route type, and profile.",
      "entry_changed": "This entry changed while the connection was being checked. Review the current values and try again.",
      "invalid_auth": "Invalid API key.",
      "invalid_host": "Invalid host. Enter a DNS name or IP address only, without a scheme, path, user info, query, or fragment.",
      "invalid_profile": "Invalid profile for the selected route type. Add-on profiles use letters, numbers, and single underscores; native profiles use lowercase letters, numbers, hyphens, and underscores.",
      "invalid_profile_route": "Invalid profile route type. Select Home Assistant add-on or native Hermes multiplexer.",
      "unknown": "An unexpected error occurred."
    },
    "step": {
      "init": {
        "data": {
          "host": "Host",
          "port": "Port",
          "profile": "Profile",
          "profile_route": "Profile route type",
          "api_key": "API Key",
          "use_ssl": "Use HTTPS",
          "verify_ssl": "Verify SSL certificate",
          "prompt": "System Prompt",
          "include_exposed_entities": "Include exposed entities in prompt",
          "context_max_chars": "Max context characters",
          "continued_conversation_mode": "Follow-up listening",
          "enable_session_reuse": "Reuse Hermes server sessions across short voice turns",
          "session_timeout_seconds": "Voice session reuse timeout (seconds)",
          "expose_device_context": "Include device/satellite context in prompt",
          "always_speak_fallback": "Always speak replies through fallback TTS",
          "fallback_media_player": "Fallback media player entity_id",
          "fallback_tts_engine": "Fallback TTS entity_id"
        },
        "data_description": {
          "profile": "Optional route name. Add-on names use letters, numbers, and single underscores; native names follow Hermes lowercase profile IDs. Leave blank for the root API.",
          "profile_route": "Home Assistant add-on uses /profile/<name>; native Hermes multiplexer uses /p/<name>. A blank Profile always uses the root API.",
          "api_key": "API key. Native multiplexer routes require the selected profile's own API key.",
          "continued_conversation_mode": "Choose when Home Assistant keeps listening after a Hermes reply: never, always, or only when Hermes asks a follow-up question."
        }
      }
    }
  }
}
